# 🐍 Деплой на PythonAnywhere (Бесплатно и просто!)

## ✅ Преимущества PythonAnywhere

- ✅ Полностью бесплатный
- ✅ Не нужна карта
- ✅ Всё через веб-интерфейс (без CLI)
- ✅ Простая настройка
- ⚠️ Ограничение: только HTTP (не HTTPS на бесплатном плане)

---

## 🚀 Шаг 1: Регистрация

1. Откройте: https://www.pythonanywhere.com
2. Нажмите **"Start running Python online in less than a minute!"**
3. Нажмите **"Create a Beginner account"**
4. Заполните форму:
   - Username: выберите любой (например: `artlitvin2008`)
   - Email: ваш email
   - Password: придумайте пароль
5. Нажмите **"Register"**
6. Подтвердите email

---

## 📦 Шаг 2: Клонировать репозиторий

### 2.1 Открыть консоль

1. После входа нажмите **"Consoles"** (вверху)
2. Нажмите **"Bash"** (создать новую консоль)

### 2.2 Клонировать код

В консоли выполните:

```bash
git clone https://github.com/artlitvin2008-alt/calorie.git
cd calorie/backend_api
```

---

## ⚙️ Шаг 3: Создать Web App

### 3.1 Перейти в Web

1. Нажмите **"Web"** (вверху)
2. Нажмите **"Add a new web app"**

### 3.2 Настроить приложение

1. **Domain**: оставьте как есть (будет `username.pythonanywhere.com`)
2. Нажмите **"Next"**
3. **Python framework**: выберите **"Manual configuration"**
4. **Python version**: выберите **"Python 3.10"**
5. Нажмите **"Next"**

---

## 🔧 Шаг 4: Настроить WSGI

### 4.1 Открыть WSGI файл

На странице Web app найдите секцию **"Code"**

Нажмите на ссылку в **"WSGI configuration file"** (например: `/var/www/username_pythonanywhere_com_wsgi.py`)

### 4.2 Заменить содержимое

Удалите всё и вставьте:

```python
import sys
import os

# Добавить путь к проекту
path = '/home/username/calorie/backend_api'
if path not in sys.path:
    sys.path.append(path)

# Установить переменные окружения
os.environ['TELEGRAM_BOT_TOKEN'] = 'ваш_токен'
os.environ['GROQ_API_KEY'] = 'ваш_ключ'
os.environ['OPENROUTER_API_KEY'] = 'ваш_ключ'
os.environ['ALLOWED_ORIGINS'] = '*'

# Импортировать приложение
from main import app as application
```

**⚠️ ВАЖНО**: 
- Замените `username` на ваш username на PythonAnywhere
- Замените значения переменных на реальные ключи

### 4.3 Сохранить

Нажмите **"Save"** (вверху справа)

---

## 📚 Шаг 5: Установить зависимости

### 5.1 Вернуться в консоль

Перейдите в **"Consoles"** → выберите вашу Bash консоль

### 5.2 Создать виртуальное окружение

```bash
cd ~/calorie/backend_api
python3.10 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

Это займёт 2-3 минуты.

---

## 🔗 Шаг 6: Настроить виртуальное окружение в Web App

### 6.1 Вернуться в Web

Перейдите в **"Web"** (вверху)

### 6.2 Указать путь к virtualenv

Найдите секцию **"Virtualenv"**

В поле **"Enter path to a virtualenv"** введите:

```
/home/username/calorie/backend_api/venv
```

**⚠️ Замените `username` на ваш username!**

Нажмите галочку (✓)

---

## 🚀 Шаг 7: Запустить приложение

### 7.1 Reload

На странице Web app нажмите большую зелёную кнопку **"Reload username.pythonanywhere.com"**

### 7.2 Получить URL

Ваш URL будет:
```
http://username.pythonanywhere.com
```

**⚠️ Замените `username` на ваш username!**

---

## ✅ Шаг 8: Проверить

### 8.1 Проверить логи

На странице Web app:
1. Прокрутите вниз до секции **"Log files"**
2. Нажмите на **"Error log"**
3. Проверьте что нет ошибок (должно быть пусто или только старые ошибки)

### 8.2 Проверить API

Откройте в браузере:
```
http://username.pythonanywhere.com/api/health
```

Должно показать:
```json
{"status":"healthy","service":"Fitness AI Coach API","version":"1.0.0"}
```

### 8.3 Если видите ошибку "No module named 'aiohttp'"

Вернитесь в Bash консоль и выполните:

```bash
cd ~/calorie/backend_api
source venv/bin/activate
pip install aiohttp python-telegram-bot httpx
```

Затем вернитесь в Web и нажмите **"Reload"**

---

## 🎯 Готово!

Backend работает на PythonAnywhere!

**Ваш Backend URL**: `http://username.pythonanywhere.com`

**⚠️ ВАЖНО для фронтенда**: 
- URL начинается с `http://` (не `https://`)
- Это может вызвать проблемы с CORS в Telegram
- Для продакшена лучше использовать платный план с HTTPS

---

## 🆘 Проблемы?

### Ошибка 502 Bad Gateway

1. Проверьте логи: Web → вкладка "Error log"
2. Убедитесь что virtualenv указан правильно
3. Проверьте что зависимости установлены

### Ошибка импорта

1. Проверьте путь в WSGI файле
2. Убедитесь что `username` заменён на реальный
3. Проверьте что вы в правильной директории

### Приложение не отвечает

1. Нажмите "Reload" ещё раз
2. Проверьте Error log
3. Убедитесь что все переменные окружения установлены

---

## 💡 Полезные ссылки

- Dashboard: https://www.pythonanywhere.com/user/username/
- Документация: https://help.pythonanywhere.com/
- Форум: https://www.pythonanywhere.com/forums/

---

**Время: 10 минут**  
**Стоимость: $0**  
**Простота: 10/10** 🚀
