# 🚀 Пошаговое руководство по деплою

## Шаг 1: Деплой Backend на Railway (5 минут)

### 1.1 Создать аккаунт на Railway
1. Перейти на https://railway.app
2. Нажать "Start a New Project"
3. Войти через GitHub

### 1.2 Создать новый проект
1. Нажать "New Project"
2. Выбрать "Deploy from GitHub repo"
3. Выбрать ваш репозиторий
4. Выбрать папку `backend_api` как root directory

### 1.3 Настроить переменные окружения
В Railway Dashboard → Variables, добавить:

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
OPENROUTER_API_KEY=ваш_ключ_openrouter
GROQ_API_KEY=ваш_ключ_groq
ALLOWED_ORIGINS=*
PORT=8000
PYTHONUNBUFFERED=1
```

### 1.4 Получить URL бэкенда
После деплоя Railway покажет URL типа:
```
https://your-app-name.railway.app
```

**Сохраните этот URL!** Он понадобится для фронтенда.

---

## Шаг 2: Деплой Frontend на Vercel (3 минуты)

### 2.1 Создать аккаунт на Vercel
1. Перейти на https://vercel.com
2. Нажать "Sign Up"
3. Войти через GitHub

### 2.2 Импортировать проект
1. Нажать "Add New..." → "Project"
2. Выбрать ваш репозиторий
3. В настройках указать:
   - **Framework Preset**: Vite
   - **Root Directory**: `miniapp-frontend`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`

### 2.3 Настроить переменные окружения
В Vercel → Settings → Environment Variables, добавить:

```
VITE_API_URL=https://your-backend-url.railway.app/api
```

**Важно**: Замените `your-backend-url.railway.app` на реальный URL из Railway!

### 2.4 Задеплоить
1. Нажать "Deploy"
2. Дождаться завершения (1-2 минуты)
3. Получить URL типа: `https://your-app.vercel.app`

**Сохраните этот URL!** Он понадобится для BotFather.

---

## Шаг 3: Обновить CORS на Backend

После получения URL фронтенда, обновить переменную в Railway:

```
ALLOWED_ORIGINS=https://your-app.vercel.app,https://web.telegram.org
```

Redeploy backend в Railway.

---

## Шаг 4: Настроить Mini App в BotFather (2 минуты)

### 4.1 Открыть @BotFather в Telegram

### 4.2 Настроить Web App
```
/mybots
→ Выбрать вашего бота
→ Bot Settings
→ Menu Button
→ Configure Menu Button
→ Ввести URL: https://your-app.vercel.app
→ Ввести название: "Open App"
```

### 4.3 Альтернативный способ - через команду
```
/setmenubutton
→ Выбрать бота
→ Ввести URL: https://your-app.vercel.app
→ Ввести текст кнопки: "Open App"
```

---

## Шаг 5: Тестирование (1 минута)

### 5.1 Открыть бота в Telegram
1. Найти вашего бота в Telegram
2. Нажать "Start" или "/start"
3. Нажать на кнопку Menu (≡) внизу экрана
4. Выбрать "Open App"

### 5.2 Проверить функциональность
- ✅ Dashboard загружается
- ✅ Можно открыть модальное окно добавления еды
- ✅ Камера работает (на телефоне)
- ✅ Навигация между экранами работает
- ✅ Тема (светлая/тёмная) переключается автоматически

---

## Альтернативные варианты деплоя

### Backend альтернативы:
- **Render.com** (бесплатный, но медленнее)
- **Fly.io** (бесплатный tier)
- **Heroku** (платный, $7/месяц)

### Frontend альтернативы:
- **Netlify** (бесплатный)
- **Cloudflare Pages** (бесплатный)
- **GitHub Pages** (бесплатный, но нужна настройка)

---

## Быстрый деплой через CLI (для опытных)

### Railway CLI
```bash
# Установить Railway CLI
npm i -g @railway/cli

# Войти
railway login

# Деплой backend
cd backend_api
railway init
railway up
railway variables set TELEGRAM_BOT_TOKEN=your_token
railway variables set ALLOWED_ORIGINS=*
```

### Vercel CLI
```bash
# Установить Vercel CLI
npm i -g vercel

# Войти
vercel login

# Деплой frontend
cd miniapp-frontend
vercel --prod
```

---

## Проверка деплоя

### Backend Health Check
```bash
curl https://your-backend-url.railway.app/api/health
```

Должен вернуть:
```json
{
  "status": "healthy",
  "service": "Fitness AI Coach API",
  "version": "1.0.0"
}
```

### Frontend Check
Открыть в браузере:
```
https://your-app.vercel.app
```

Должна загрузиться страница с Dashboard.

---

## Troubleshooting

### Backend не запускается
- Проверить логи в Railway Dashboard
- Убедиться, что все переменные окружения установлены
- Проверить, что `requirements.txt` содержит все зависимости

### Frontend показывает ошибки API
- Проверить `VITE_API_URL` в Vercel
- Убедиться, что backend доступен
- Проверить CORS настройки на backend

### Mini App не открывается в Telegram
- Убедиться, что URL правильный в BotFather
- Проверить, что frontend доступен по HTTPS
- Попробовать очистить кэш Telegram (Settings → Data and Storage → Clear Cache)

### Камера не работает
- Убедиться, что сайт использует HTTPS
- Проверить разрешения браузера/Telegram
- Попробовать на другом устройстве

---

## Следующие шаги после деплоя

1. ✅ Протестировать все функции через телефон
2. ⏳ Добавить видео-анализ (Phase 4)
3. ⏳ Настроить синхронизацию с ботом
4. ⏳ Добавить PWA функции
5. ⏳ Оптимизировать производительность

---

## Полезные ссылки

- Railway Dashboard: https://railway.app/dashboard
- Vercel Dashboard: https://vercel.com/dashboard
- Telegram Bot API: https://core.telegram.org/bots/api
- Mini Apps Documentation: https://core.telegram.org/bots/webapps

---

**Готово!** Теперь ваш Mini App доступен через Telegram на любом устройстве! 🎉
