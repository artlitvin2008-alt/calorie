# 🧪 Руководство по тестированию Telegram Mini App

## Текущий статус

✅ **Backend API**: 100% готов  
✅ **Frontend**: 100% готов  
⏳ **Требуется**: Настройка окружения и запуск серверов

## Что нужно для тестирования

### 1. Установить Node.js и npm (если ещё не установлены)

```bash
# Проверить установку
node --version
npm --version

# Если не установлены, установить через Homebrew (macOS)
brew install node
```

### 2. Установить зависимости фронтенда

```bash
cd miniapp-frontend
npm install
```

### 3. Запустить Backend API

```bash
# В корневой директории проекта
cd backend_api
python3 -m uvicorn main:app --reload --port 8000
```

Backend будет доступен на `http://localhost:8000`  
Swagger документация: `http://localhost:8000/docs`

### 4. Запустить Frontend Dev Server

```bash
# В другом терминале
cd miniapp-frontend
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

## Тестирование в браузере (локально)

После запуска обоих серверов:

1. Откройте `http://localhost:5173` в браузере
2. Вы увидите Mini App интерфейс
3. **Проблема**: Telegram WebApp SDK не будет работать вне Telegram

### Обход для локального тестирования

Создайте mock для Telegram WebApp SDK:

```javascript
// Добавить в miniapp-frontend/index.html перед </head>
<script>
  window.Telegram = {
    WebApp: {
      initData: 'mock_init_data',
      initDataUnsafe: {
        user: {
          id: 123456789,
          first_name: 'Test',
          username: 'testuser'
        }
      },
      colorScheme: 'light',
      themeParams: {},
      ready: () => console.log('WebApp ready'),
      expand: () => console.log('WebApp expanded'),
      close: () => console.log('WebApp closed'),
    }
  }
</script>
```

## Тестирование через Telegram (полноценное)

Для тестирования через телефон нужно:

### 1. Деплой Backend API

Варианты:
- **Railway.app** (бесплатный tier)
- **Render.com** (бесплатный tier)
- **Heroku** (платный)
- **VPS** (DigitalOcean, AWS, etc.)

### 2. Деплой Frontend

Варианты:
- **Vercel** (рекомендуется, бесплатный)
- **Netlify** (бесплатный)
- **GitHub Pages**

### 3. Настроить Mini App в BotFather

```
1. Открыть @BotFather в Telegram
2. Отправить /mybots
3. Выбрать вашего бота
4. Bot Settings → Menu Button → Configure Menu Button
5. Указать URL вашего фронтенда (например, https://your-app.vercel.app)
```

### 4. Открыть Mini App

В чате с ботом:
- Нажать на кнопку Menu (≡) рядом с полем ввода
- Или отправить команду `/start` и нажать на кнопку

## Быстрый старт для локального тестирования

### Вариант 1: С mock данными (без бэкенда)

```bash
cd miniapp-frontend
npm install
npm run dev
```

Добавить mock в `index.html` (см. выше)

### Вариант 2: С полным стеком

**Терминал 1 - Backend:**
```bash
cd backend_api
python3 -m uvicorn main:app --reload --port 8000
```

**Терминал 2 - Frontend:**
```bash
cd miniapp-frontend
npm install
npm run dev
```

Открыть `http://localhost:5173`

## Проверка работоспособности

### Backend API

```bash
# Health check
curl http://localhost:8000/api/health

# Должен вернуть:
# {"status":"healthy","service":"Fitness AI Coach API","version":"1.0.0"}
```

### Frontend

1. Откройте `http://localhost:5173`
2. Должны увидеть экран Dashboard
3. Проверьте консоль браузера на ошибки

## Следующие шаги для полноценного тестирования

1. ✅ Установить Node.js и npm
2. ✅ Установить зависимости (`npm install`)
3. ✅ Запустить оба сервера
4. ⏳ Задеплоить на хостинг
5. ⏳ Настроить в BotFather
6. ⏳ Протестировать через Telegram на телефоне

## Известные ограничения локального тестирования

- ❌ Telegram WebApp SDK не работает вне Telegram
- ❌ Камера может не работать без HTTPS
- ❌ Некоторые Telegram-специфичные функции недоступны
- ✅ Можно тестировать UI и логику
- ✅ Можно тестировать API запросы

## Рекомендация

Для полноценного тестирования **рекомендую сразу задеплоить**:

1. Backend на Railway.app (5 минут)
2. Frontend на Vercel (3 минуты)
3. Настроить в BotFather (2 минуты)
4. Тестировать через телефон

Хотите, чтобы я помог с деплоем?
